Closes #

<!-- A repository chore — documentation, CI configuration, a dependency bump — may replace the line above with a one-line reason instead of an issue number. -->

## What this changes

<!-- The behavior delivered, not the files touched. -->

## Validation

<!-- The commands that were run and their results. `npm run check` is the minimum. Changes to the Compose stack, the images, or the deployment scripts also need `npm run test:compose`; changes to the TLS profile also need `npm run test:tls-config`. -->

```bash

```

## Follow-up

<!-- Work this deliberately leaves out, or "None". Do not fold unrelated changes in here. -->

## Checklist

- [ ] The title, description, and code are in English.
- [ ] New behavior is covered by automated tests, or this change adds no behavior.
- [ ] `npm run check` passes locally, along with any deployment check the change requires.
- [ ] No credentials, uploaded media, databases, or local environment files are committed.
- [ ] This pull request covers one issue, or one self-contained chore, and nothing else.
